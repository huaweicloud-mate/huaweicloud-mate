# 线程交接说明

更新时间：2026-07-13  
工作目录：`D:\CodeSpace\huaweicloud-mate`
当前阶段：M0 契约冻结进行中；尚未授权应用工程实现
需求理解度：约 93%

## 0. 新线程 60 秒摘要

- 架构已从 Proposed v0.2 收敛为用户确认的 Proposed v0.3-lite。
- 首版仍覆盖四类 Agent、产品 MCP、KooCLI 和全部风险类型操作，但删除企业级治理和过度抽象。
- Agent 只看到 `search / describe / execute` 三个 Router tools。
- 产品 MCP 静态随 npm 内置，默认 MCP 优先，未覆盖时在执行前选择 KooCLI，执行开始后不自动切换。
- 危险操作仍由 `execute` 两阶段处理，但第二阶段必须携带固定受信签发器产生的 `approvalReceipt`；Agent 自己回传 token 不再视为用户确认。
- 首版使用单账号权限受限 `credentials.json`；AK/SK 可通过同域 HTTPS 进入官方产品 MCP 的最长 15 分钟内存 session。
- credentials 使用 generation；跨 Core 不承诺全部 session 立即撤销，遗留 session 最迟 900 秒失效；route token 保证多实例路由。
- 四宿主使用声明式模板；单一 npm package；无动态 Registry、mTLS、信封加密、通用 Adapter SPI 或 Policy Engine。
- ADR-0008 已接受，`docs/契约/` 已生成 M0 Draft schema 与测试向量；没有应用工程代码或 `package.json`。
- 用户已授权继续 M0 文档与契约工作，但未授权创建 TypeScript 工程代码、提交或推送 Git。

新线程优先读取本文件，然后读取 `task_plan.md`、`docs/技术架构.md`、`docs/安全架构.md`、`docs/首版实施路线图.md`。不需要重新调研已经确认的架构取舍。

## 1. 项目目标

构建一个通过公共 npm 一键安装的开源华为云 Agent 插件，让用户可以直接指挥 Agent 创建、修改、查询和删除华为云资源，例如创建并配置 ECS、OBS、IAM 和网络资源。

首版支持：

- OpenCode；
- Claude Code；
- Codex；
- 华为云码道；
- 内置华为云产品 MCP；
- 自动安装或复用 KooCLI；
- 创建、修改、删除、IAM、网络和计费操作。

## 2. Proposed v0.3-lite 已确认决策

### 产品与发行

1. 单一 TypeScript npm package，通过公共 npm 发布。
2. 建议入口：`npx @huaweicloud/agent-plugin install`。
3. 默认用户级安装，不要求管理员权限。
4. 自动配置四类 Agent，不接受仅输出配置片段。
5. 首版不支持离线、HTTP/HTTPS 代理和 headless Linux。
6. `npx` 只作为安装入口；长期运行时物化到固定用户目录并通过稳定 launcher 启动。

### Tool Router

Agent 只看到三个固定工具：

```text
cloud_capabilities_search
cloud_capability_describe
cloud_action_execute
```

`status` 由 CLI `doctor/auth status` 承担；`plan` 合并到 execute 两阶段确认。

禁止：

- 任意 JavaScript/Python 执行；
- 任意 endpoint、命令或可执行路径；
- 通过 Tool 参数传入 AK/SK；
- 绕过能力清单的通用 OpenAPI 调用。

### Provider 与路由

1. 产品 MCP 和 capability 数据随 npm 静态内置。
2. 不建设动态 Registry、签名、吊销或远程 denylist。
3. 不支持社区、第三方或用户自定义 Provider。
4. Tool 参数中的执行器选择只视为不可信偏好，Router 仍执行 capability、健康和风险校验。
5. 未指定时产品 MCP 支持且健康则默认 MCP，否则在执行前选择 KooCLI。
6. 生成 preview 或开始执行后锁定 executor。
7. 执行失败、超时或结果未知时不自动切换。

### 危险操作

- 只有普通 read 校验后直接执行；
- write 以及 `destructive / privileged / cost / sensitive-read` 复合风险首次 execute 返回 preview、摘要和 challenge；
- 固定受信签发器在明确用户操作后生成 `approvalReceipt`；
- receipt 绑定参数、executor、凭证代次、账号和作用域；Router 在 dispatch 前原子消费 preview。

### 凭证

1. 首版只有一个当前账号。
2. 用户首次使用时通过 `auth set` 输入 AK/SK，可覆盖更新或删除。
3. AK/SK 保存到用户目录下权限受限的独立 `credentials.json`。
4. credentials 带随机 generation，Core 在执行和复用 session 前检查代次。
5. `auth set` 通过固定只读身份校验能力绑定非敏感 account/domain identity。
6. 不实现 OS Keyring。
7. 产品 MCP 通过同域 HTTPS credential session 接收 AK/SK。
8. Provider 只在内存保存最长 15 分钟，数据面使用 opaque session ID 和 route token。
9. `auth set/remove` 对已知 session 尽力撤销，不承诺无共享控制面的全局立即撤销。
10. 不建设独立凭证控制面、设备 mTLS、信封加密或 KMS/HSM。
11. 明确信任内置官方产品 MCP 及其 HTTPS 终止链路可以接触明文 AK/SK。

### KooCLI

1. 优先检测用户预装 KooCLI。
2. 版本兼容且 doctor 通过时复用。
3. 否则下载插件声明的固定版本，校验 SHA-256 后安装到私有用户目录。
4. 安装后执行版本、网络、认证和最小只读测试。
5. 不修改系统 KooCLI，不加入 PATH。

### 宿主适配与日志

1. 四宿主使用声明式模板，不设计通用 Adapter SPI。
2. 首版只提供 install、doctor、uninstall；重跑 install 完成升级。
3. 保留必要配置备份、失败回滚和最小 install-state。
4. 不提供独立 repair 或复杂 drift 检测。
5. 只记录本地脱敏 JSONL 日志，不上传遥测。
6. 四宿主模板必须绑定固定审批 issuer 和稳定 launcher。

## 3. 当前架构

```text
四类 Agent
  -> 声明式 MCP 配置 + Canonical Skills
  -> 本地 stdio Tool Router
       -> 静态能力清单
       -> 复合风险门禁 + 可信 approval receipt
       -> Product MCP Client -> 官方产品 MCP
       -> KooCLI Adapter -> 预装或私有 KooCLI

本地共享：
  单账号 credentials 文件
  credentials generation
  最小 install-state
  本地脱敏 JSONL 日志
```

Core 随 Agent session 启动，不安装常驻 daemon。

## 4. v0.2 中已删除或延后

- 五工具面中的 status 和独立 plan；
- 动态签名 Provider Registry；
- 产品 MCP 与 KooCLI 完全同级的复杂路由；
- 独立 Policy Engine；
- 独立凭证控制面；
- 设备 mTLS、信封加密和 KMS/HSM；
- OS Secret Store 和多账号 target；
- 通用 Adapter SPI；
- Provider conformance 平台；
- 分布式审计、企业策略中心和遥测；
- 多 package monorepo；
- update/repair 和复杂 drift 框架。

ADR-0003 和 ADR-0006 已标记为被 ADR-0007 取代。

## 5. 正式文档索引

- [技术架构](技术架构.md)
- [智能体宿主适配规范](智能体适配器接口规范.md)
- [产品 MCP 接入规范](产品MCP接入规范.md)
- [安全架构](安全架构.md)
- [首版实施路线图](首版实施路线图.md)
- [架构决策记录](架构决策/)
- [M0 机器可读契约](契约/README.md)

工作过程记录：

- `D:\CodeSpace\huaweicloud-mate\task_plan.md`
- `D:\CodeSpace\huaweicloud-mate\findings.md`
- `D:\CodeSpace\huaweicloud-mate\progress.md`

## 6. PoC 边界

`D:\CodeSpace\AI-Plugin` 只是 OBS MCP 预研 Demo，可只读参考，不能作为正式基线。

不能继承：

- MetaMCP 身份混淆；
- 开发机绝对路径；
- 运行时 `npx -y`；
- 任意代码 `mcp_execute`；
- Skill 与环境变量的写权限策略漂移。

可以参考测试思路和紧凑工具面经验。

## 7. Mermaid 状态

正式技术架构使用无 flowchart edge-label 的主图，避免宿主放大后连线标签变黑框。凭证会话使用独立 sequence diagram。

当前环境没有本地 Mermaid CLI。已完成代码块闭合、相对链接、旧术语和 edge-label 扫描；像素级渲染仍只能在实际文档宿主中复核。

## 8. Git 与仓库状态

- 分支：`master`；
- 最新提交：`e80b188 docs: simplify open-source v1 architecture`；
- 当前存在 M0 文档修改和此前已有的 `docs/架构方案.zip` 删除记录；
- 未创建工程代码、`package.json` 或项目骨架；
- 本轮未执行提交或 push。

## 9. 实施前仍需输入

以下内容不改变核心架构：

- 公共 npm scope 与发布身份；
- 首发产品 MCP 清单和同源 endpoint；
- 产品 MCP capability 复合风险、输出策略和 schema digest；
- 产品 MCP credential session 责任人；
- `auth set` 固定只读账号身份校验能力和责任人；
- 四宿主可信审批签发方式、issuer ID 和责任人；
- KooCLI 兼容范围、固定版本和 SHA-256；
- 3～5 个端到端验收场景。

## 10. 推荐下一步

1. 新线程先执行 `git status --short` 和 `git log -1 --oneline`，识别 M0 文档修改和既有 zip 删除记录。
2. 继续校验并评审 `docs/契约/`，在外部输入补齐前保持 Draft。
3. 冻结四宿主审批 issuer、产品 MCP 握手/session 路由、KooCLI 真实版本与 3～5 个验收场景。
4. 未经新的明确授权，不要创建 TypeScript 工程代码、提交或推送 Git。

## 11. 新线程启动提示词

```text
继续 D:\CodeSpace\huaweicloud-mate 的华为云 Agent 插件工作。

请完整阅读：
1. docs\线程交接说明.md
2. task_plan.md
3. docs\技术架构.md
4. docs\安全架构.md
5. docs\首版实施路线图.md

当前架构是用户已确认的 Proposed v0.3-lite，并由 ADR-0008 补齐安全闭环。M0 契约冻结进行中。不要恢复 v0.2 中已删除的动态 Registry、独立凭证控制面、mTLS、信封加密、通用 Adapter SPI 或五工具设计。

先检查 git status 和最新提交。可以继续 M0 文档与契约校验；未经用户明确授权，不要创建 TypeScript 工程代码、提交或推送 Git。
```
