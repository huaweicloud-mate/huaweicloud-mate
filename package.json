{
  "name": "huaweicloud-mate",
  "version": "0.0.0-development",
  "private": true,
  "description": "Huawei Cloud agent plugin development package",
  "type": "module",
  "engines": {
    "node": ">=22 <25"
  },
  "bin": {
    "huaweicloud-mate": "dist/cli.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/clean.mjs && tsc -p tsconfig.json && node scripts/copy-contracts.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "doctor": "npm run build && node dist/cli.js doctor --contracts-only"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
